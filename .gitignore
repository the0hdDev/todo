# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules/           # Ignoriert alle node_modules-Ordner, egal wo sie liegen
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
coverage/

# next.js
.next/
out/                    # Für Next.js Static HTML Export

# production build (falls es einen separaten build-Ordner gibt)
build/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
.env.local
.env.development.local
.env.production.local

# vercel deployment files
.vercel/

# typescript
*.tsbuildinfo
# next-env.d.ts # Diese Datei normalerweise NICHT ignorieren, da sie von Next.js generiert wird und für Typen wichtig ist.